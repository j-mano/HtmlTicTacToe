// This array is used to mark tha postition and what state the card are in. 0 is null value or not an selected card. 1 is tick and 2 is tack
var possArray = [
    [0, 0, 0],
    [0, 0, 0],
    [0, 0, 0],
];

// true = cross, false = circle;
var currentplayer = true;